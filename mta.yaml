_schema-version: "3.2"
ID: appsdflcexpensemanager
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appsdflcexpensemanager-destination-content
  type: com.sap.application.content
  requires:
  - name: appsdflcexpensemanager-destination-service
    parameters:
      content-target: true
  - name: appsdflcexpensemanager-repo-host
    parameters:
      service-key:
        name: appsdflcexpensemanager-repo-host-key
  - name: appsdflcexpensemanager-uaa
    parameters:
      service-key:
        name: appsdflcexpensemanager-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appsdflcexpensemanager_html_repo_host
          ServiceInstanceName: appsdflcexpensemanager-html5-service
          ServiceKeyName: appsdflcexpensemanager-repo-host-key
          sap.cloud.service: appsdflcexpensemanager
        - Authentication: OAuth2UserTokenExchange
          Name: appsdflcexpensemanager_uaa
          ServiceInstanceName: ExpenseManager-uaa
          ServiceKeyName: appsdflcexpensemanager-uaa-key
          sap.cloud.service: appsdflcexpensemanager
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appsdflcexpensemanager-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appsdflcexpensemanager-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appsdflcexpensemanager.zip
      name: appsdflcexpensemanager
      target-path: resources/
- name: appsdflcexpensemanager
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appsdflcexpensemanager-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            Name: ExpenseManager
            ProxyType: Internet
            Type: HTTP
            URL: https://orgname-dev-expensemanager-srv.cfapps.us10-003.hana.ondemand.com
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appsdflcexpensemanager-destination-service
    service-plan: lite
- name: appsdflcexpensemanager-uaa
  type: org.cloudfoundry.existing-service
  parameters:
    service-name: ExpenseManager-uaa
- name: appsdflcexpensemanager-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appsdflcexpensemanager-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
